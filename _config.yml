# GitHub Pages Configuration
theme: null
markdown: kramdown

# Prevent Jekyll from processing these files
include:
  - content

# Disable Jekyll processing for markdown files
keep_files:
  - content

# Set proper MIME types
defaults:
  - scope:
      path: "content"
    values:
      layout: null
